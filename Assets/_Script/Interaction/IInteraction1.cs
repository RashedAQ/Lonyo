using UnityEngine;
public interface IInteraction
{
    void Interact();
    string GetPrompt();
}
public class IInteraction1 : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created

}
